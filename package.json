{
  "name": "rollup-preset-solid",
  "version": "2.0.1",
  "description": "Rollup preset for solid libraries",
  "private": true,
  "scripts": {
    "build": "pnpm run --filter @bastant/rollup build",
    "watch": "pnpm run --filter @bastant/rollup watch"
  }
}
